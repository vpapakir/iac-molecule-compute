parameters:
- name: condition
  type: string

stages:
- stage: CreatePR
  displayName: 'Create Release PR'
  dependsOn: Test
  condition: ${{ parameters.condition }}
  variables:
  - group: shared
  jobs:
  - template: ../jobs/create-pr.yml